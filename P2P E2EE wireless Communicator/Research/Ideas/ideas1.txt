I want to add a few more features where in whilst now we're limited to a certain character space of the input string, we should have some sort of an infinite scroll feature with which the input string can be as long as we want and the display of the inputted text scrolls (or moves right as and when needed) dynamically as per the requirements, this should allow for no need of any kind of limit on the input size.

Alongside this, we have to add a toggle to see into the system utilizations like that in htop or windows's performance viewer or some sort of game-like testing overlay that shows the real-time utilization of various resources like CPU, memory, heap, I/O read-writes etc we can reference some publicly available libraries for the same or adapt our own versions from the GNU programs etc. Note that this mode would be toggled or set into the esp32 by the use of a switch that would be connected to some GPIO pin say D5 and to the gnd on the other end, such that when the switch is pressed, it continuously sends a signal on that specific GPIO and immediately switches the OLED's output mode to that of the resource utilization/ systems monitoring mode.

