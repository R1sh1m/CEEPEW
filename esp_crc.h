/* esp_crc.h - root-level stub */

#ifndef ESP_CRC_H_ROOT
#define ESP_CRC_H_ROOT

#include <stddef.h>
#include <stdint.h>

static inline uint32_t esp_crc32_le(uint32_t crc, const void *buf, size_t size) { (void)crc; (void)buf; (void)size; return 0U; }

#endif /* ESP_CRC_H_ROOT */
